# react-native-custom-boilerplate

This package is used to create a custom boilerplate with our own bundle identifier.

To Use this boilerplate, use the below commmand

npx react-native-custom-boilerplate
